---
import Icon from '~/components/Icon.astro';
import { flagships } from '~/lib/site';
---

<section id="flagships" class="py-10 md:py-12 lg:py-16 border-y border-border/60 bg-surface/30">
  <div class="container-app">
    <div class="max-w-2xl reveal">
      <p class="eyebrow"><Icon name="layers" size={14} /> Flagships</p>
      <h2 class="mt-4">Two flagships. One operating mode.</h2>
      <p class="mt-5 text-fluid-lg text-muted">
        Glitch Executor is the productized end-to-end execution surface —
        the moat is the locked playbooks and tuned models.
        Mesh Pilot is the open agent mesh — the moat is the orchestration
        brain, not the individual agents. Pick the one closest to your problem.
      </p>
    </div>

    <ul class="mt-14 grid gap-6 md:grid-cols-2" role="list">
      {flagships.map((b) => (
        <li class="card p-6 md:p-8 reveal group relative flex flex-col h-full
                   hover:border-brand/40 transition-colors">
          <div aria-hidden="true"
               class="pointer-events-none absolute inset-0 -z-10 opacity-0 group-hover:opacity-100
                      transition-opacity duration-500 rounded-[inherit]
                      bg-[radial-gradient(circle_at_top,rgb(var(--color-brand)/0.08),transparent_70%)]">
          </div>

          <div class="flex items-center gap-3">
            <span class="inline-flex size-11 items-center justify-center rounded-md bg-brand/10 text-brand border border-brand/20">
              <Icon name={b.icon} size={22} />
            </span>
            <div>
              <h3 class="text-fluid-xl leading-none">{b.name}</h3>
              <p class="mt-1 font-mono text-fluid-xs text-subtle">{b.domainCaption}</p>
            </div>
          </div>

          <p class="mt-5 text-fluid-base text-fg/90">{b.tagline}</p>

          <ul class="mt-5 space-y-2 text-fluid-sm" role="list">
            {b.proof.map((p) => (
              <li class="flex items-start gap-2">
                <span class="mt-[3px] text-brand"><Icon name="check" size={14} /></span>
                <span class="text-muted">{p}</span>
              </li>
            ))}
          </ul>

          <div class="mt-auto pt-6">
            <a
              href={b.href}
              rel="noopener"
              class="inline-flex items-center gap-1.5 text-brand text-fluid-sm font-medium
                     underline decoration-brand/30 hover:decoration-brand min-h-11 py-2"
              data-cta={`flagship-${b.slug}`}
            >
              Visit {b.name}
              <Icon name="arrow-up-right" size={14} />
            </a>
          </div>
        </li>
      ))}
    </ul>
  </div>
</section>
